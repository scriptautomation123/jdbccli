# Testcontainers configuration
# Explicitly use Unix socket strategy
docker.client.strategy=org.testcontainers.dockerclient.UnixSocketClientProviderStrategy
# Disable checks for alternative strategies
checks.disable=true
# Reuse containers between test runs for faster execution
testcontainers.reuse.enable=true
